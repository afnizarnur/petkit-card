import { TemplateResult } from "lit";
import { LitterBoxEntities } from "../types";
export declare function renderLitterBox(entities: LitterBoxEntities, title: string | undefined, devicePrefix: string, handleToggle: (ev: CustomEvent) => void, handleClick: (ev: CustomEvent) => void): TemplateResult;
